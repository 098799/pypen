Django==6.0.8
django-admin-sortable2==2.2.3
django-allauth==65.16.1
django-extensions==3.2.3
django-money==3.5.3
djangorestframework==3.17.2
whitenoise==6.7.0

anthropic==0.96.0
google-genai==1.73.1
pillow==12.3.0

python-dotenv==1.2.2
pandas==2.2.2
tabulate==0.9.0
termcolor==2.4.0
typing_extensions==4.15.0

# django-money builds each currency field's `choices` from Babel's CLDR data, so
# an unpinned Babel makes `migrate` print "models have changes not reflected in
# a migration" wherever its version differs from the one the migrations were
# made with (bae had 2.16.0, p340 2.18.0 — found 2026-09-18). No schema effect.
Babel==2.18.0

# Explicit pins for transitives that had advisories in older versions.
# Floors raised 2026-09-25 from pip-audit of bae's venv; tests_security.py
# checks the installed versions against the same list.
setuptools>=78.1.1
sqlparse>=0.6.0
urllib3>=2.7.0
idna>=3.15
pyasn1>=0.6.4
anyio>=4.14.2

# allauth's Google provider imports these, but allauth declares them only as
# the `socialaccount` extra — they were on bae by hand and missing from a fresh
# install, so `manage.py` could not even start. PyJWT checks Google's id_token.
PyJWT[crypto]>=2.13.0
cryptography>=48.0.1
requests>=2.33.1

black==24.8.0
isort==5.13.2
mypy==1.11.2
mypy-extensions==1.0.0
importmagic==0.1.7
ipdb==0.13.13
ipython==8.27.0
jedi==0.19.1

pytest==8.3.2
pytest-django==4.9.0
pytest-sugar==1.0.0
